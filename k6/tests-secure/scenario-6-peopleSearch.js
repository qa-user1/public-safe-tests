import Searches from '../helpers/secure/secure-search-helper.js';

export function peopleSearch (virtualUserIndex, token) {
    
    // Search People
    var result = Searches.searchPeople(virtualUserIndex, token);

}