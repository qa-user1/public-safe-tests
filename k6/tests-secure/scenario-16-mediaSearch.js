import Searches from '../helpers/secure/secure-search-helper.js';


export function mediaSearch (virtualUserIndex, token) {
    
    // Search Media
    var result = Searches.searchMedia(virtualUserIndex, token);

}