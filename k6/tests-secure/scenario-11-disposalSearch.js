import Searches from '../helpers/secure/secure-search-helper.js';

export function disposalSearch (virtualUserIndex, token) {

    // Search Disposals
    var result = Searches.searchDisposals(virtualUserIndex, token);
}